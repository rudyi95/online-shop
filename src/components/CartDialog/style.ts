import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  root: {},
  cartIcon: {
    color: "white",
    marginRight: 20,
  },
  btnGroup: {
    display: "flex",
    justifyContent: "space-around",
    minWidth: 360,
  },
});
